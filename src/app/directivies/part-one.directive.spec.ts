import { PartOneDirective } from './part-one.directive';

describe('PartOneDirective', () => {
  it('should create an instance', () => {
    const directive = new PartOneDirective();
    expect(directive).toBeTruthy();
  });
});
